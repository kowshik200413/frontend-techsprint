# .env.example - example environment variables for Farm Vaidya
# Copy to `.env` (or set these values in your hosting provider) and replace placeholders

# Pipecat public or server token used in the demo. For production, use a server-side minting flow instead.
VITE_PIPECAT_TOKEN=pk_aff3af37-4821-4efc-9776-1f2d300a52d0
# Optional: override the Pipecat endpoint
VITE_PIPECAT_ENDPOINT=https://api.pipecat.daily.co/v1/public/methane-agent/start

# Base URL where the app will be served (useful for analytics, absolute links)
VITE_APP_BASE_URL=https://voice.example.com

# Port used in Docker preview (if running locally with docker-compose)
PORT=5173

# NODE_ENV
NODE_ENV=production
